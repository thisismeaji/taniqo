---
import EmptyCategoryState from "../components/EmptyCategoryState.astro";
import PostCard from "../components/PostCard.astro";
import Layout from "../layouts/Layout.astro";
import { getArticleCards } from "../lib/articles";

const posts = getArticleCards("tips-dan-trik");
const seo = {
	title: "Tips dan Trik Berkebun | Taniqo",
	description: "Kumpulan tips dan trik berkebun dari Taniqo untuk merawat tanaman rumahan, menjaga kebun kecil, dan mengatasi masalah tanaman harian.",
	canonicalPath: "/tips-dan-trik",
	ogTitle: "Tips dan Trik Berkebun",
	ogDescription: "Temukan cara sederhana merawat tanaman, menjaga kebun tetap sehat, dan menyelesaikan masalah berkebun rumahan.",
	image: "/images/thumbnail/dummy-thumbnail.png",
	imageAlt: "Kumpulan tips dan trik berkebun Taniqo",
	robots: "index, follow",
};
const canonicalUrl = new URL(seo.canonicalPath, Astro.site ?? Astro.url.origin).toString();
const collectionSchema = {
	"@context": "https://schema.org",
	"@type": "CollectionPage",
	name: seo.ogTitle,
	description: seo.description,
	url: canonicalUrl,
	isPartOf: {
		"@type": "WebSite",
		name: "Taniqo",
	},
	mainEntity: posts.map((post, index) => ({
		"@type": "ListItem",
		position: index + 1,
		url: new URL(post.href, Astro.site ?? Astro.url.origin).toString(),
		name: post.title,
	})),
};
---

<Layout
	title={seo.title}
	description={seo.description}
	canonicalUrl={canonicalUrl}
	image={seo.image}
	imageAlt={seo.imageAlt}
	ogTitle={seo.ogTitle}
	ogDescription={seo.ogDescription}
	robots={seo.robots}
>
	<script slot="head" type="application/ld+json" set:html={JSON.stringify(collectionSchema)} />
	<main class="relative z-0 bg-white py-8 text-zinc-950">
		<section class="mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8">
			{posts.length > 0 ? (
				<div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
					{posts.map((post) => <PostCard {...post} />)}
				</div>
			) : (
				<EmptyCategoryState category="Tips dan Trik" />
			)}
		</section>
	</main>
</Layout>
